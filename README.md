# lfr-client-extension-maven

How to create a Liferay client extension module for a maven workspace.

This module will create a LUFFA zip to expose a JS as a Liferay GlobalJS Client Extension.

This module should be put in an existing Liferay Maven Workspace.
